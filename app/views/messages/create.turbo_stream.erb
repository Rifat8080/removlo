<%= turbo_stream.append "messages", partial: "messages/message", locals: { message: @message } %>
<%= turbo_stream.replace "message_form", partial: "conversations/message_form", locals: { conversation: @conversation, message: @conversation.messages.new } %>
